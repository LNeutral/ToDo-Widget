{
  "name": "todo-widget",
  "version": "1.0.0",
  "description": "Desktop To-Do Widget",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "set WIN_SKIP_SIGNING=true && electron-builder"
  },
  "build": {
    "appId": "com.todowidget.app",
    "productName": "Todo Widget",
    "directories": {
      "buildResources": "assets"
    }
  },
  "dependencies": {
    "auto-launch": "^5.0.6",
    "electron-store": "^8.1.0"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1"
  }
}
